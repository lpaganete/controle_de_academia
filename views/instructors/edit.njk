
{% extends "layout.njk" %}


{% block content %}
    <!-- Sobrescrevendo o método POST pelo metodo PUT-->
    <form class="card" method="POST" action="/instructors?_method=PUT"> 
    
         <section class="avatar" 
                 style="background: url({{ instructor.avatar_url }}) no-repeat center center / cover">
        </section>

        <section class="details">
           
            <h3>Editar Instrutor</h3>

            {% include 'instructors/fields.njk' %} <!--Vai incluir um pedaço de código que está no arquvo fields.njk-->

        <input type="hidden" name="id" value="{{ instructor.id}}"> <!-- Input que não parece que -->

        </section>
    </form>

{% endblock %}